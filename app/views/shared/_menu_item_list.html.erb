<%# locals: (item:, restaurant:) %>
<% theme = restaurant.theme || {} %>
<% text_color = theme['textColor'] || '#1f2937' %>
<% primary_color = theme['primaryColor'] || '#000000' %>

<button class="flex w-full items-center justify-between border-b py-4 text-left last:border-b-0"
  style="border-color: <%= text_color %>10"
  data-controller="item-modal"
  data-action="click->item-modal#open"
  data-item-modal-item-value="<%= item.to_json %>">
  <div class="min-w-0 flex-1">
    <h3 class="font-semibold"><%= item.name %></h3>
    <% if item.description.present? %>
      <p class="mt-0.5 truncate text-sm opacity-60"><%= item.description %></p>
    <% end %>
  </div>
  <p class="ml-4 shrink-0 font-bold" style="color: <%= primary_color %>">
    <%= number_to_currency(item.price, unit: "€", format: "%u%n") %>
  </p>
</button>
